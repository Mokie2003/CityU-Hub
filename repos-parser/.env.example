# CityU-Hub 解析器环境变量（复制为 .env 后按需修改）

# GitHub 个人访问令牌（可选）。不配置也能抓公开仓库，但匿名限额低（60 次/小时）
GITHUB_TOKEN=

GITHUB_TIMEOUT_MS=10000

# 输入 / 输出目录（可选，默认分别是 <repo>/repos 与 <repo>/web/public/data）
# REPOS_DIR=
# OUTPUT_DIR=
